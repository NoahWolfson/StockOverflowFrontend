/**
 * this type represents the stock ticker symbol which will be in the url. This ticker is importnatn to get teh stock information from the backend api's to the frontend 
 */
export type StockPageParams = {
    stockTicker?: string; 
}